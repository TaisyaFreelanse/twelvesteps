# План доработки проекта 12 шагов

## 1. Настройка локальной базы данных PostgreSQL

### 1.1 Docker Compose конфигурация

- ✅ Создать `docker-compose.yml` в корне проекта с PostgreSQL сервисом
- ✅ Настроить переменные окружения для БД (пользователь, пароль, база данных)
- ✅ Обновить `backend.env` с локальными параметрами подключения

### 1.2 Обновление конфигурации

- ✅ Обновить `backend.env`: заменить DATABASE_URL на локальный (postgresql+asyncpg://postgres:password@localhost:5432/twelvesteps)
- ✅ Создать `.env.example` с шаблонами переменных окружения

## 2. Модели базы данных для "Рассказать о себе"

### 2.1 Новые модели в `twelvesteps/db/models.py`

- ✅ **ProfileSection** - базовые разделы профиля (Семья, Друзья, и т.д.)
- ✅ id, name (с эмодзи), icon, is_custom, user_id (для кастомных), order_index
- ✅ **ProfileSectionData** - данные пользователя по разделам
- ✅ id, user_id, section_id, content (JSON или Text), created_at, updated_at
- ✅ **ProfileQuestion** - вопросы для разделов
- ✅ id, section_id, question_text, order_index, is_optional
- ✅ **ProfileAnswer** - ответы пользователя на вопросы
- ✅ id, user_id, question_id, answer_text, created_at, version

### 2.2 Миграция Alembic

- ✅ Создать миграцию для новых таблиц
- ✅ Добавить начальные данные для стандартных разделов (14 разделов из спецификации)

## 3. Backend API для "Рассказать о себе"

### 3.1 Новые схемы в `twelvesteps/api/schemas.py`

- ✅ `ProfileSectionSchema`, `ProfileSectionDataSchema`
- ✅ `ProfileQuestionSchema`, `ProfileAnswerSchema`
- ✅ `ProfileSectionListResponse`, `ProfileSectionDetailResponse`
- ✅ `FreeTextRequest` - для свободного рассказа
- ✅ Дополнительно: `ProfileAnswerRequest`, `CustomSectionRequest`, `SectionUpdateRequest`, `SectionSummaryResponse`

### 3.2 Новые эндпоинты в `twelvesteps/api/main.py`

- ✅ `GET /profile/sections` - список всех разделов
- ✅ `GET /profile/sections/{section_id}` - детали раздела с вопросами
- ✅ `POST /profile/sections/{section_id}/answer` - сохранение ответа на вопрос
- ✅ `POST /profile/sections/{section_id}/free-text` - свободный рассказ
- ✅ `POST /profile/sections/custom` - создание кастомного раздела
- ✅ `GET /profile/sections/{section_id}/summary` - сводка по разделу
- ✅ `PUT /profile/sections/{section_id}` - редактирование раздела

### 3.3 Сервисный слой

- ✅ Создать `twelvesteps/services/profile.py`:
- ✅ `ProfileService` с методами для работы с разделами
- ✅ Логика FSM для последовательности вопросов (`get_fsm_state`, `get_next_question_for_section`)
- ✅ Автоматическое предложение следующего вопроса на основе ответа (`_suggest_next_question` с использованием LLM)
- ✅ Разбивка длинных ответов (>4096 символов) (`split_long_message`)

### 3.4 Репозитории

- ✅ Создать `twelvesteps/repositories/ProfileRepository.py` для работы с профильными данными

## 4. Telegram Bot - "Рассказать о себе"

### 4.1 FSM состояния в `twelvesteps_tgbot/bot/handlers.py`

- ✅ Создать `ProfileStates` (StatesGroup):
- ✅ `section_selection` - выбор раздела
- ✅ `answering_question` - ответ на вопрос
- ✅ `free_text_input` - свободный ввод
- ✅ `creating_custom_section` - создание кастомного раздела

### 4.2 Обработчики команд

- ✅ `/profile` - главное меню разделов (показать все разделы горизонтально)
- ✅ Обработчики для каждого раздела с кнопками (`handle_profile_callback`)
- ✅ Обработчик свободного рассказа (`handle_profile_free_text`)
- ✅ Обработчик создания кастомного раздела (`handle_profile_custom_section`)
- ✅ Обработчик ответов на вопросы (`handle_profile_answer`)

### 4.3 Клавиатуры в `twelvesteps_tgbot/bot/config.py`

- ✅ `build_profile_sections_markup()` - сетка всех разделов (горизонтальная раскладка, 2 кнопки в ряд)
- ✅ `build_profile_actions_markup()` - [✍️ Свободный рассказ] [✅ Сохранить] [⏪ Назад]
- ✅ `build_profile_skip_markup()` - для пропуска вопросов (inline кнопка "⏭ Пропустить")

### 4.4 Интеграция с бэкендом

- ✅ Добавить методы в `twelvesteps_tgbot/bot/backend.py`:
- ✅ `get_profile_sections()`, `get_section_detail()`
- ✅ `submit_profile_answer()`, `submit_free_text()`
- ✅ `create_custom_section()`, `update_section()`, `delete_section()`
- ✅ `get_section_summary()` (дополнительно)

### 4.5 Обработка длинных сообщений

- ✅ Создать утилиту `split_long_message()` в `twelvesteps_tgbot/bot/utils.py`:
- ✅ Разбивка текста >4096 символов на части (по абзацам, предложениям, словам)
- ✅ Сохранение форматирования и контекста
- ✅ Вспомогательные функции `send_long_message()` и `edit_long_message()`
- ✅ Использовать в handlers при отправке ответов пользователю (handle_message, handle_sos, профильные обработчики)

## 5. Улучшение "Работа по шагу"

### 5.1 Шаблоны ответов

- ✅ Создать модель `AnswerTemplate` в `twelvesteps/db/models.py`:
- ✅ id, user_id, name, template_type (AUTHOR/CUSTOM), structure (JSON)
- ✅ Enum `TemplateType` с значениями AUTHOR и CUSTOM
- ✅ Структура авторского шаблона: Ситуация → Мысли → Чувства (до) → Действия → Чувства (после) → Пути выхода → Вывод → Что не попало (хранится в JSON поле structure)

### 5.2 API для шаблонов

- ✅ `GET /steps/templates` - список доступных шаблонов (авторский + пользовательские)
- ✅ `POST /steps/templates` - создание пользовательского шаблона
- ✅ `PUT /steps/templates/{template_id}` - редактирование шаблона
- ✅ `PATCH /me/template` - выбор активного шаблона
- ✅ Создан `AnswerTemplateRepository` для работы с шаблонами
- ✅ Создан `TemplateService` для бизнес-логики шаблонов

### 5.3 Обновление User модели

- ✅ Добавить поле `active_template_id` в модель User
- ✅ Миграция Alembic для нового поля и таблицы answer_templates
- ✅ Создание начального авторского шаблона в миграции

### 5.4 UI для выбора шаблона

- ✅ При первом входе в `/steps` - предложить выбор шаблона (проверка active_template_id)
- ✅ Кнопки: [🧩 Авторский шаблон] [✍️ Свой шаблон] (inline клавиатура)
- ✅ Сохранение выбора в профиле пользователя через API (PATCH /me/template)
- ✅ Обработчик `handle_template_selection` для выбора шаблона
- ✅ Методы в `BackendClient`: `get_templates()`, `set_active_template()`

### 5.5 Контекстная помощь (🆘 Нужна помощь)

- ✅ Улучшить существующий `/sos` handler в `twelvesteps_tgbot/bot/handlers.py`
- ✅ Создать FSM состояние `SosStates` для диалога помощи
- ✅ Добавить inline-кнопки выбора типа помощи:
- ✅ [📌 Не понимаю вопрос]
- ✅ [🧱 Не могу вспомнить ситуацию]
- ✅ [🔁 Застрял — не могу сформулировать]
- ✅ [😶 Просто тяжело, нужна поддержка]
- ✅ [✍️ Своё]
- ✅ Интеграция с GPT через новый эндпоинт `POST /sos/chat`:
- ✅ Принимает тип помощи и контекст текущего вопроса
- ✅ Возвращает индивидуальный диалог
- ✅ Разговор НЕ сохраняется как ответ шага по умолчанию
- ✅ В конце спрашивает: "Сохранить этот разговор как черновик?" (да/нет)
- ✅ После выхода из SOS - возврат к текущему вопросу шага
- ✅ Создан `SosService` для обработки SOS chat диалогов
- ✅ Добавлены клавиатуры: `build_sos_help_type_markup()`, `build_sos_save_draft_markup()`, `build_sos_exit_markup()`
- ✅ Обработчики: `handle_sos_callback()`, `handle_sos_custom_input()`, `handle_sos_chat_message()`

### 5.6 Навигация по шагам

- ✅ Обновить `/steps` handler:
- ✅ Показывать текущий шаг (номер шага)
- ✅ Кнопка [🔢 Выбрать другой шаг] → inline-список шагов 1-12
- ✅ [📋 Показать список вопросов] - inline-список вопросов текущего шага
- ✅ API эндпоинты: GET /steps/current, GET /steps/list, GET /steps/{step_id}/questions
- ✅ Методы в StepFlowService: get_current_step_info(), get_all_steps(), get_step_questions()
- ✅ Методы в BackendClient: get_current_step_info(), get_all_steps(), get_step_questions()
- ✅ Клавиатуры: build_steps_navigation_markup(), build_steps_list_markup(), build_step_questions_markup()
- ✅ Обработчики: handle_steps_navigation_callback(), handle_step_selection_callback(), handle_question_view_callback()

### 5.7 Дополнительные кнопки в режиме шагов

- ✅ [🧩 Заполнить по шаблону] - структурированный ввод по шаблону (FSM для заполнения каждого поля шаблона)
- ✅ [⏸ Пауза] - сохранение черновика в Tail.payload, закрытие Tail не происходит
- ✅ [🔁 Перейти к другому вопросу] - inline-список вопросов текущего шага для выбора
- ✅ [📜 Вернуться к предыдущему] - переход к предыдущему вопросу (если есть сохраненный ответ, показать его)
- ✅ [➕ Добавить ещё] - для повторения последнего микрошага/добавления элемента (заглушка)
- ✅ API эндпоинты: POST /steps/draft, GET /steps/draft, GET /steps/question/{id}/previous, POST /steps/switch-question
- ✅ Методы в StepFlowService: save_draft(), get_active_tail_draft(), get_previous_answer(), switch_to_question()
- ✅ Методы в BackendClient: save_draft(), get_draft(), get_previous_answer(), switch_to_question()
- ✅ Клавиатура: build_step_actions_markup() с кнопками действий
- ✅ FSM состояния: StepState.filling_template, StepState.template_field
- ✅ Обработчики: handle_step_action_callback(), handle_template_field_input()

## 6. Настройки "Работа по шагу"

### 6.1 Эндпоинты настроек

- ✅ `GET /steps/settings` - текущие настройки пользователя
- ✅ `PUT /steps/settings` - обновление настроек (шаблон, напоминания)
- ✅ Создан `StepsSettingsService` для работы с настройками
- ✅ Схемы: `StepsSettingsResponse`, `StepsSettingsUpdateRequest`
- ✅ Методы в `BackendClient`: `get_steps_settings()`, `update_steps_settings()`
- ✅ Поддержка настройки активного шаблона
- ✅ Поддержка настроек напоминаний (структура готова, реализация в User модели будет позже)

### 6.2 Команда настроек в боте

- ✅ `/steps_settings` - меню настроек работы по шагу
- ✅ Просмотр/смена активного шаблона (выбор из списка доступных шаблонов)
- ✅ Редактирование пользовательского шаблона (меню выбора, полное редактирование будет позже)
- ✅ Сброс на авторский шаблон (одной кнопкой)
- ✅ Настройка напоминаний (включение/выключение, структура для времени и дней готова)
- ✅ Клавиатуры: build_steps_settings_markup(), build_template_selection_settings_markup(), build_reminders_settings_markup()
- ✅ Обработчики: handle_steps_settings(), handle_steps_settings_callback()

## 7. Обновление существующего кода

### 7.1 Обновление `twelvesteps/api/steps.py`

- ✅ Интеграция с шаблонами при сохранении ответов (получение активного шаблона пользователя)
- ✅ Логика заполнения по шаблону (структурированный JSON в answer_text с валидацией)
- ✅ Метод `get_current_step_questions()` для получения списка вопросов текущего шага
- ✅ Метод `switch_to_question()` для перехода к конкретному вопросу (уже был реализован ранее)
- ✅ Обновлен `save_user_answer()` с поддержкой `is_template_format` флага
- ✅ Обновлен `AnswerRequest` схема с полем `is_template_format`
- ✅ Добавлен эндпоинт `GET /steps/current/questions` для получения вопросов текущего шага

### 7.2 Обновление `twelvesteps_tgbot/bot/handlers.py`

- ✅ Улучшение существующих handlers для шагов (использование send_long_message для длинных ответов)
- ✅ Добавление новых handlers для профиля (уже были реализованы ранее)
- ✅ Интеграция разбивки длинных сообщений во все ответы бота (замена message.answer и callback.message.edit_text на send_long_message и edit_long_message)
- ✅ Добавлены обработчики handle_template_field_input и handle_step_action_callback для работы с шаблонами
- ✅ Все длинные сообщения теперь автоматически разбиваются на части

### 7.3 Обновление `twelvesteps/services/status.py`

- ✅ Реализовать полноценный `get_status_for_user()`:
- ✅ Определение текущего шага и его статуса (поиск UserStep со статусом IN_PROGRESS)
- ✅ Поиск открытого вопроса (Tail с tail_type=STEP_QUESTION и is_closed=False)
- ✅ Возврат корректной структуры StatusResponse (current_step, current_step_status, open_step_question, tails)
- ✅ Получение всех открытых tails пользователя

### 7.4 Обновление `twelvesteps/core/chat_service.py`

- ✅ Добавить метод для обработки свободного рассказа профиля (`process_profile_free_text`)
- ✅ Использовать существующий `analyze_profile` для разложения текста по разделам
- ✅ Реализована логика распределения информации по разделам профиля с помощью LLM
- ✅ Метод сохраняет информацию в соответствующие разделы через ProfileService

## 8. Исправление багов из отчета о тестировании

### 8.1 Баг #001: Отсутствие кнопки "Начать заново" после сбоя

- ✅ Добавить кнопку "Начать заново" (/start) в интерфейс при ошибках (build_error_markup в config.py)
- ✅ Реализовать команду /reset или /restart для сброса состояния диалога (handle_reset в handlers.py)
- ✅ При ошибках автоматически предлагать вариант перезапуска: "Произошла ошибка. Хотите начать заново?"
- ✅ Обновить обработчик ошибок в `twelvesteps_tgbot/bot/onboarding.py` (handle_self_description)
- ✅ Добавить кнопку /start в клавиатуру при ошибках в `twelvesteps_tgbot/bot/config.py`
- ✅ Обновлены все обработчики ошибок в handlers.py для использования build_error_markup

### 8.2 Баг #002: Отсутствие индикатора текущего шага

- ✅ Добавить визуальный индикатор прогресса в `/steps`: "Шаг 3 из 12: Принятие решения" (format_step_progress_indicator)
- ✅ Кратко описывать суть текущего шага перед вопросами (step_description отображается в handle_steps)
- ✅ Реализовать прогресс-бар или текстовый счетчик: "Вопрос 5 из 7 в этом шаге" (answered_questions/total_questions)
- ✅ Добавить команду `/about_step` для возврата к описанию текущего шага (handle_about_step)
- ✅ Обновить `twelvesteps/api/steps.py` для возврата информации о шаге (номер, название, описание, прогресс)
- ✅ Обновить `twelvesteps_tgbot/bot/handlers.py` (handle_steps, handle_step_answer) для отображения индикатора
- ✅ Добавить поля `title` и `description` в модель `Step` для хранения названия и описания шага
- ✅ Добавлен эндпоинт GET /steps/{step_id}/detail для получения детальной информации о шаге

### 8.3 Баг #003: Бот не использует контекст из текущей сессии

- ✅ Реализовать сохранение контекста сессии (/steps, /day) в базу данных
- ✅ Создать модель `SessionContext` в `twelvesteps/db/models.py`:
- ✅ id, user_id, session_type (STEPS/DAY/CHAT), context_data (JSON), created_at, updated_at
- ✅ Обновить `handle_chat` для использования контекста текущей сессии
- ✅ Интегрировать контекст сессии в промпты для LLM
- ✅ Обновить `twelvesteps/core/chat_service.py` для учета сессионного контекста
- ✅ При вызове /day или /steps сохранять контекст в SessionContext (реализовано для /steps)
- ✅ При обычном чате загружать активный контекст сессии и использовать его в промпте
- ✅ Создан репозиторий SessionContextRepository для работы с контекстом
- ✅ Добавлены API эндпоинты POST /session/context и GET /session/context
- ✅ Создана миграция b7c8d9e0f1a2_add_session_context.py

### 8.4 Баг #005: Команда /thanks генерирует похожий ответ как /day

- ✅ Разделить логику генерации ответов для /day и /thanks
- ✅ Создать отдельные промпты для каждой команды в `twelvesteps/llm/prompts/`:
- ✅ `thanks.json` - промпт для команды /thanks (поддержка, мотивация, благодарность)
- ✅ `day.json` - промпт для команды /day (анализ текущего состояния)
- ✅ Реализовать проверку последней команды для исключения дублирования
- ✅ Добавить уникальные пулы фраз для /thanks (поддержка, мотивация, благодарность)
- ✅ Обновить обработчик /thanks в `twelvesteps_tgbot/bot/handlers.py`
- ✅ Создать отдельный метод в `twelvesteps/core/chat_service.py` для обработки /thanks
- ✅ Сохранять последнюю команду пользователя в SessionContext для проверки дублирования
- ✅ Добавлены методы load_thanks_prompt() и load_day_prompt() в PromptRepository
- ✅ Созданы отдельные эндпоинты POST /thanks и POST /day в API
- ✅ Добавлены методы thanks() и day() в BackendClient

### 8.5 Баг #006: Отсутствие валидации ввода на этапе регистрации

- ✅ Добавить валидацию контекста регистрации в `twelvesteps_tgbot/bot/onboarding.py`
- ✅ Реализовать проверку на вопросы (начинается с вопросительного слова или содержит фразы типа "расскажи о себе", "что ты умеешь")
- ✅ При обнаружении вопроса: ответить кратко, вернуть контекст регистрации
- ✅ Повторно запросить данные для профиля после ответа на вопрос
- ✅ Обновить `handle_self_description` для валидации ввода
- ✅ Создать утилиту `is_question()` в `twelvesteps_tgbot/bot/utils.py` для определения вопросов
- ✅ При обнаружении вопроса показать сообщение: "Это кратко о моих функциях. А теперь, пожалуйста, расскажи о себе, чтобы я мог лучше тебя понимать"

## 9. Интеграция структуры данных GPT_SELF_UJSON_v3_full.md

### 9.1 Расширение модели User

- ✅ Добавить поля в `twelvesteps/db/models.py`:
- ✅ `relapse_dates` (JSON массив дат в формате YYYY-MM-DD)
- ✅ `sponsor_ids` (JSON массив ID спонсоров)
- ✅ `custom_fields` (JSON для целей, важных людей, формата поддержки)
- ✅ `last_active` (timestamp, обновляется при каждом взаимодействии)
- ✅ Создать миграцию Alembic для новых полей
- ✅ Обновить `twelvesteps/api/schemas.py` для включения новых полей в UserSchema

### 9.2 Модель SessionState для оперативного состояния

- ✅ Создать модель `SessionState` в `twelvesteps/db/models.py`:
- ✅ id, user_id (ForeignKey), recent_messages (JSON массив с timestamp, text, tags)
- ✅ daily_snapshot (JSON: emotions, triggers, actions, health)
- ✅ active_blocks (JSON массив строк)
- ✅ pending_topics (JSON массив строк)
- ✅ group_signals (JSON массив строк)
- ✅ created_at, updated_at
- ✅ Связь с User через ForeignKey
- ✅ Создать миграцию Alembic

### 9.3 Расширение модели Frame для отслеживания фреймов

- ✅ Расширить существующую модель `Frame` или создать `FrameTracking`:
- ✅ `confirmed` (JSON массив подтвержденных фреймов)
- ✅ `candidates` (JSON массив кандидатов)
- ✅ `tracking` (JSON: repetition_count объект, min_to_confirm число)
- ✅ `archetypes` (JSON массив архетипов: victim, rescuer, judge и т.д.)
- ✅ `meta_flags` (JSON массив: loop_detected, frame_shift, identity_conflict)
- ✅ Связь с User
- ✅ Создать миграцию Alembic

### 9.4 Модель QAStatus для контроля качества

- ✅ Создать модель `QAStatus` в `twelvesteps/db/models.py`:
- ✅ id, user_id (ForeignKey)
- ✅ `last_prompt_included` (boolean)
- ✅ `trace_ok` (boolean)
- ✅ `open_threads` (integer, количество незакрытых веток)
- ✅ `rebuild_required` (boolean)
- ✅ created_at, updated_at
- ✅ Связь с User через ForeignKey
- ✅ Создать миграцию Alembic

### 9.5 Модель UserMeta для системных данных

- ✅ Создать модель `UserMeta` в `twelvesteps/db/models.py`:
- ✅ id, user_id (ForeignKey, unique)
- ✅ `metasloy_signals` (JSON массив строк)
- ✅ `prompt_revision_history` (integer, количество пересмотров промпта)
- ✅ `time_zone` (string, например "UTC+3")
- ✅ `language` (string, default='ru')
- ✅ `data_flags` (JSON: encrypted boolean, anonymized boolean, retention_days integer)
- ✅ created_at, updated_at
- ✅ Связь с User через ForeignKey (один к одному)
- ✅ Создать миграцию Alembic

### 9.6 Модель TrackerSummary для дневного наблюдения

- ✅ Создать модель `TrackerSummary` в `twelvesteps/db/models.py`:
- ✅ id, user_id (ForeignKey)
- ✅ `thinking` (JSON массив строк)
- ✅ `feeling` (JSON массив строк)
- ✅ `behavior` (JSON массив строк)
- ✅ `relationships` (JSON массив строк)
- ✅ `health` (JSON массив строк)
- ✅ `date` (date, для группировки по дням)
- ✅ created_at, updated_at
- ✅ Связь с User через ForeignKey
- ✅ Уникальный индекс на (user_id, date) для одного свода в день
- ✅ Создать миграцию Alembic

### 9.7 API для работы с расширенными данными

- ✅ Добавить эндпоинты в `twelvesteps/api/main.py`:
- ✅ `GET /user/state` - получение оперативного состояния (SessionState)
- ✅ `POST /user/state` - обновление состояния
- ✅ `GET /user/frames` - получение фреймов и трекинга (FrameTracking)
- ✅ `POST /user/frames` - обновление фреймов
- ✅ `GET /user/qa-status` - получение статуса QA
- ✅ `POST /user/qa-status` - обновление статуса QA
- ✅ `GET /user/tracker-summary` - получение сводки трекера (по дате или последняя)
- ✅ `POST /user/tracker-summary` - сохранение сводки
- ✅ `GET /user/meta` - получение метаданных пользователя
- ✅ `PUT /user/meta` - обновление метаданных

### 9.8 Сервисы для работы с расширенными данными

- ✅ Создать `twelvesteps/services/state_service.py`:
- ✅ Методы для работы с SessionState
- ✅ Логика обновления daily_snapshot на основе классификации
- ✅ Управление active_blocks и pending_topics
- ✅ Добавление сообщений в recent_messages с тегами
- ✅ Создать `twelvesteps/services/frame_tracking_service.py`:
- ✅ Логика подтверждения фреймов (min_to_confirm, обычно 3)
- ✅ Отслеживание повторений (repetition_count)
- ✅ Определение архетипов на основе паттернов
- ✅ Обнаружение meta_flags (loop_detected, frame_shift, identity_conflict)
- ✅ Создать `twelvesteps/services/tracker_service.py`:
- ✅ Генерация tracker_summary на основе данных дня
- ✅ Агрегация данных по категориям (thinking, feeling, behavior, relationships, health)
- ✅ Метод для получения сводки за период

### 9.9 Интеграция с существующей системой

- Обновить `twelvesteps/core/chat_service.py`:
- Сохранение recent_messages в SessionState при каждом сообщении
- Обновление daily_snapshot на основе классификации (emotions, triggers, actions, health)
- Интеграция active_blocks из классификации в SessionState
- Загрузка SessionState при начале чата и использование в контексте
- Обновить `twelvesteps/llm/openai_provider.py`:
- Использование SessionState в контексте промптов
- Учет FrameTracking при генерации ответов (избегание повторений, учет архетипов)
- Использование TrackerSummary для персонализации ответов
- Учет pending_topics и group_signals в промптах

### 9.10 Репозитории для новых моделей

- ✅ Создать `twelvesteps/repositories/SessionStateRepository.py`
- ✅ Создать `twelvesteps/repositories/FrameTrackingRepository.py`
- ✅ Создать `twelvesteps/repositories/QAStatusRepository.py`
- ✅ Создать `twelvesteps/repositories/UserMetaRepository.py`
- ✅ Создать `twelvesteps/repositories/TrackerSummaryRepository.py`

## 10. Тестирование и документация

### 10.1 Тестирование

- ✅ Создан подробный чеклист тестирования в `TESTING_CHECKLIST.md`
- Проверка работы всех новых эндпоинтов
- Тестирование FSM состояний в боте
- Проверка миграций базы данных
- Тестирование исправлений багов из отчета
- Проверка работы расширенной структуры данных GPT_SELF_UJSON_v3
- Тестирование интеграции контекста сессий
- Проверка валидации ввода на этапе регистрации

### 10.2 Документация

- Обновить README с инструкциями по запуску Docker Compose
- Описание новых команд бота
- Документация API эндпоинтов
- Описание структуры данных GPT_SELF_UJSON_v3
- Документация по исправленным багам
- Описание работы с контекстом сессий
- Инструкции по настройке расширенных данных пользователя

## Файлы для изменения/создания

**Новые файлы:**
- `docker-compose.yml`
- `.env.example`
- `twelvesteps/services/profile.py`
- `twelvesteps/services/state_service.py`
- `twelvesteps/services/frame_tracking_service.py`
- `twelvesteps/services/tracker_service.py`
- `twelvesteps/repositories/ProfileRepository.py`
- `twelvesteps/repositories/SessionStateRepository.py`
- `twelvesteps/repositories/FrameTrackingRepository.py`
- `twelvesteps/repositories/QAStatusRepository.py`
- `twelvesteps/repositories/UserMetaRepository.py`
- `twelvesteps/repositories/TrackerSummaryRepository.py`
- `twelvesteps/alembic/versions/XXX_add_profile_sections.py`
- `twelvesteps/alembic/versions/XXX_add_answer_templates.py`
- `twelvesteps/alembic/versions/XXX_add_gpt_self_structure.py`
- `twelvesteps/alembic/versions/XXX_add_session_context.py`
- `twelvesteps_tgbot/bot/utils.py` - утилиты (разбивка сообщений, валидация и т.д.)
- `twelvesteps/llm/prompts/thanks.json` - промпт для команды /thanks
- `twelvesteps/llm/prompts/day.json` - промпт для команды /day

**Изменяемые файлы:**
- `backend.env` - обновить DATABASE_URL
- `twelvesteps/db/models.py` - добавить новые модели (ProfileSection, SessionState, FrameTracking, QAStatus, UserMeta, TrackerSummary, AnswerTemplate, SessionContext)
- `twelvesteps/api/main.py` - новые эндпоинты (включая `/sos/chat`, `/user/state`, `/user/frames` и т.д.)
- `twelvesteps/api/schemas.py` - новые схемы
- `twelvesteps/api/steps.py` - интеграция с шаблонами, методы навигации, возврат информации о шаге
- `twelvesteps/services/status.py` - реализация get_status_for_user
- `twelvesteps/core/chat_service.py` - метод обработки свободного рассказа, интеграция SessionState
- `twelvesteps/llm/openai_provider.py` - использование расширенных данных в промптах
- `twelvesteps_tgbot/bot/handlers.py` - новые handlers, улучшение существующих, исправления багов
- `twelvesteps_tgbot/bot/config.py` - новые клавиатуры, кнопка /start при ошибках
- `twelvesteps_tgbot/bot/backend.py` - новые методы API клиента
- `twelvesteps_tgbot/bot/onboarding.py` - валидация ввода, обработка ошибок с кнопкой перезапуска