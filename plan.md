# План разработки: Финализация MVP и монетизация

## Текущая архитектура

Проект состоит из:

- **Backend API** (`twelvesteps/`): FastAPI приложение с SQLAlchemy, PostgreSQL, ChromaDB
- **Telegram Bot** (`twelvesteps_tgbot/`): aiogram 3.x бот
- **База данных**: PostgreSQL с моделями User, StepAnswer, ProfileAnswer, Frame, Gratitude и др.
- **Развертывание**: Render (production), Docker Compose (local)



## Этап 1: Инфраструктура и CI/CD

**Стоимость**: 6,000 - 12,000 руб

### 1.2 Структура GitHub репозитория

**Цель**: Организовать код для удобной работы команды и CI/CD

**Структура**:

```
twelvesteps/
├── .github/
│   └── workflows/
│       ├── deploy-backend.yml
│       └── deploy-bot.yml
├── docs/
│   ├── API.md
│   ├── DEPLOYMENT.md
│   ├── ARCHITECTURE.md
│   └── PAYMENT_INTEGRATION.md
├── twelvesteps/          # Backend API
├── twelvesteps_tgbot/    # Telegram Bot
├── docker-compose.yml
├── .env.example
├── README.md
├── .gitignore
└── LICENSE
```

**Файлы для создания**:

- `README.md` - описание проекта, установка, запуск
- `docs/API.md` - документация API endpoints
- `docs/DEPLOYMENT.md` - инструкции по деплою на Render
- `docs/ARCHITECTURE.md` - архитектура системы
- `.github/workflows/deploy-backend.yml` - GitHub Actions для автодеплоя backend
- `.github/workflows/deploy-bot.yml` - GitHub Actions для автодеплоя bot

**GitHub Actions workflow** (пример для Render):

```yaml
# .github/workflows/deploy-backend.yml
name: Deploy Backend to Render
on:
  push:
    branches: [main]
    paths:
   - 'twelvesteps/**'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
   - uses: actions/checkout@v3
   - name: Trigger Render Deploy
        run: |
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
```

---

### 1.3 Автодеплой на Render

**Текущее состояние**: Проект развернут на Render

**Задачи**:

1. Настроить Webhook для автодеплоя при push в main ветку
2. Настроить переменные окружения в Render Dashboard
3. Настроить health checks для автоматического перезапуска
4. Настроить логирование (Render Logs)

**Файлы для изменения**:

- `twelvesteps/Dockerfile` - убедиться в корректности
- `twelvesteps_tgbot/Dockerfile` - убедиться в корректности
- `render.yaml` (новый) - конфигурация для Render Blueprint (опционально)

**Переменные окружения Render**:

- `DATABASE_URL` - PostgreSQL connection string
- `OPENAI_API_KEY` - OpenAI API ключ
- `BOT_TOKEN` - Telegram Bot Token
- `ENCRYPTION_KEY` - ключ шифрования
- `BACKEND_API_BASE_URL` - URL backend API для bot
- `CHROMADB_PATH` - путь к ChromaDB (или использовать облачный вариант)

**Health Check endpoints**:

- Backend: `GET /health` - проверить подключение к БД
- Bot: внутренний health check через webhook

---

## Этап 2: Монетизация (Payment Integration)

**Общая стоимость**: 24,000 - 36,000 руб

### 2.1 Модель подписки в БД

**Стоимость**: 6,000 руб

Цель Добавить систему подписок с привязкой к Telegram ID

Изменения в БД

- Создать таблицу `subscriptions`
- Добавить поля в `User` модель для быстрого доступа

Файлы для создания/изменения

- `twelvesteps/db/models.py` - добавить модель `Subscription`
- `twelvesteps/alembic/versions/XXX_add_subscriptions.py` - миграция
- `twelvesteps/repositories/SubscriptionRepository.py` - CRUD операции
- `twelvesteps/api/schemas.py` - Pydantic схемы для подписок
- `twelvesteps/api/main.py` - endpoints для подписок

Модель Subscription

```python
class Subscription(Base):
    __tablename__ = "subscriptions"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), unique=True, index=True)
    status: Mapped[str] = mapped_column(String(20))  # active, expired, cancelled
    plan: Mapped[str] = mapped_column(String(20))  # free, premium
    started_at: Mapped[datetime]
    expires_at: Mapped[Optional[datetime]]
    payment_provider: Mapped[Optional[str]]  # yookassa, cryptomus
    payment_id: Mapped[Optional[str]]  # ID платежа в системе оплаты
    created_at: Mapped[datetime]
    updated_at: Mapped[datetime]
```

**Логика подписки**:

Первые три месяца бесплатно,

3. Базовая цена 390 рублей / PRO цена 690 рублй

Базовая:        Работа по шагам — неограниченно
		Самоанализ (10 шаг) — доступен
		Чувства и благодарности — доступны
		Один SOS-запрос в день с использование джипити 4о мини ( ограничение по токенам )
		Свободный чат (джипити 4о мини 3 запроса в день ограничение по токенам не привязан к системе памяти фреймов и тп) вопрос какое поставить ограничение и как то надо видеть расходы)
		Доступ к своим ответам и истории — обязательно
                Кнопка нужна удалить аккаунт
                
                


В Pro:
		Неограниченные запросы в SOS - GPT mini 5
		GPT‑5-mini — как модель по умолчанию в Сос
		Умный свободный чат (с памятью + фреймы и тп..)
                счетчик трезвости
                Голосовой ввод 
                блок литература АН
                

Расширенная Аналитика\Сводка за последние 7\30 дней  Блок Пример

📅 Активность	                       12 дней из 14 работал по шагам
🎯 Шаги	                               Пройдено 28 вопросов из 68 в шаге 1
🔁 Частота эмоций	               Чаще всего — тревога, вина, злость
💬 SOS	                               Запрашивал помощь 6 раз, на темы: границы, страх
📚 Благодарности	               Писал 8 раз за 14 дней, часто — про семью
🔎 Повторяющиеся темы	               «Одиночество», «Контроль», «Безответственность»
📈 Изменения за период	               Ранее писал о вине, сейчас чаще — о свободе



                Просмотр ядра личности  ниже блок пример
               
🧭 Основные ценности 	                «Свобода», «честность», «независимость» 
🧱 Часто активируемые фреймы 	        «Изоляция», «Контроль», «Стыд»
💬 Преобладающие эмоции	                «Тревога», «Обида», «Надежда»
🔁 Карта паттернов.         	        «Откладываю важное», «Ищу одобрение»
🧠 Привычные установки	                «Меня должны спасать», «Я никому не нужен»
🧍 Сформированная роль	                «Спасатель», «Изолированный», «Контролирующий»
Определений может быть больше или меньше чем 3 шт( возможно имеет смысл реализовать после реализации блока логика - нужен коментарий. 

                
                Запрос - Расскажи как я изменился за 30 дней ( нужен промт)
                Запрос - На что я закрываю глаза, какие мои точки роста (нужен промт)




- Middleware для проверки подписки
- Декораторы для ограничения доступа
- UI индикаторы платных функций в боте

**Файлы для изменения**:

- `twelvesteps/api/dependencies.py` - добавить `require_premium` dependency
- `twelvesteps_tgbot/bot/middleware.py` - middleware для проверки подписки
- Все handlers платных функций - добавить проверку подписки


		
 


### 2.2 Интеграция ЮKassa (Россия)

**Стоимость**: 12,000 - 18,000 руб

**Цель**: Прием платежей через ЮKassa для российских пользователей

**Документация**: https://yookassa.ru/developers/api

**Файлы для создания**:

- `twelvesteps/services/payment/yookassa_service.py` - сервис работы с ЮKassa API
- `twelvesteps/api/payment.py` - endpoints для обработки платежей
- `twelvesteps/api/schemas.py` - схемы для платежей

**Flow платежа**:

1. Пользователь нажимает "Оформить подписку" в боте
2. Backend создает платеж через ЮKassa API
3. Пользователь переходит на страницу оплаты ЮKassa
4. После оплаты ЮKassa отправляет webhook на наш backend
5. Backend проверяет платеж и активирует подписку

**Endpoints**:

- `POST /payment/yookassa/create` - создать платеж
- `POST /payment/yookassa/webhook` - обработка webhook от ЮKassa
- `GET /payment/yookassa/status/{payment_id}` - проверка статуса

**Переменные окружения**:

- `YOOKASSA_SHOP_ID` - ID магазина
- `YOOKASSA_SECRET_KEY` - секретный ключ



### 2.4 Интеграция подписки в Telegram Bot

**Стоимость**: 6,000 - 12,000 руб

**Цель**: Добавить UI для оформления и управления подпиской в боте

**Файлы для изменения**:

- `twelvesteps_tgbot/bot/config.py` - добавить кнопки подписки в меню
- `twelvesteps_tgbot/bot/handlers.py` - обработчики для подписки
- `twelvesteps_tgbot/bot/backend.py` - методы для работы с payment API

**UI в боте**:

- Кнопка "💎 Подписка" в главном меню
- Меню подписки: статус, оформить, продлить, отменить
- Уведомления о скором истечении подписки

**Проверка подписки**:

- Middleware для проверки подписки перед доступом к платным функциям
- Декоратор `@require_premium` для handlers

Доработка Логики  Необходима проверить атуальность и жизнеспособность данной реализации

---

## Этап 2.5: Семантическое ядро GPT-SELF (доработка логики)

**Стоимость**: 18,000 - 30,000 руб

**Примечание**: Требуется проверка актуальности и жизнеспособности реализации

СЕМАНТИЧЕСКОЕ ЯДРО GPT‑SELF
===========================

Semantic Core = смысловая структура личности в системе

1. Схема смысловой сборки

                 ┌──────────────┐
                 │  Input Text  │ ← сообщение пользователя
                 └──────┬───────┘
                        │
     ┌──────────────────┴──────────────────────┐
     │                                         │
     ▼                                         ▼
┌──────────────┐                       ┌─────────────────┐
│ Framing      │                       │ Emotion Engine   │
│ Engine       │                       │ (basic + nuance) │
│              │                       └─────────────────┘
│ → определяет:│                               │
│   • Фрейм    │                               ▼
│   • Блок     │                     ┌─────────────────────┐
│   • Мотивац. │                     │ Tagged Emotions      │
│   • Контекст │                     │ (“тревога + контроль”)│
└──────┬───────┘                     └─────────────────────┘
       │
       ▼
┌────────────────────────────┐
│ DeepTagMap (Semantic Tags) │ ← семантические теги  
│ • Темы (семья, работа)     │  
│ • Повторяющиеся паттерны   │  
│ • Родственные связи тегов  │  
└────────┬───────────────────┘
         │
         ▼
 ┌───────────────────────────────┐
 │ Semantic Blocks (Контейнеры) │
 │ • Собирают схожие фреймы     │
 │ • Агрегируют паттерны        │
 │ • Формируют структуру ядра   │
 └──────────┬────────────────────┘
            ▼
    🧠 Semantic Core (Ядро личности)
    • Устойчивые паттерны
    • Слабые/сильные стороны
    • Повторяющиеся темы
    • Ценности / страхи / конфликты
    • История шагов и ответов


2. Пример сборки (на практике)

Сообщение: «Я снова не смог сказать нет на работе, чувствовал вину, как всегда...»

Происходит:
- Framing Engine → Фрейм: избегание + блок "страх отказа"
- Emotions → Вина, тревога
- Tags → работа, границы, самоценность
- Semantic Block → тема «Нарушение границ + Перегруз»
- Semantic Core обновляется: усиливается зона "работа + вина + избегание"


3. Что включается в Personalized Prompt:
- Устойчивые темы и паттерны пользователя
- Актуальные фреймы
- Эмоции и блоки из ядра
- Последний контекст (chat/day/step)
- Цели и смыслы из «О себе» (если заданы)


4. Задачи для доработки (Артёму)

| Блок              | Что требуется |
|-------------------|---------------|
| Framing Engine    | Добавить мотивации, сопротивление, страх |
| Emotion Engine    | Добавить нюансные эмоции (стыд, вина, заморозка) |
| TagMap            | Настроить иерархию тегов и их связи |
| Semantic Block    | Создать логическую группировку фреймов и тегов |
| Semantic Core     | Собрать из всех выше — единое ядро личности |
| Prompt Builder    | Интегрировать semantic core в personalized prompt |


+ к логике 

🔧 Что нужно доработать (с пояснениями для ТЗ):

⸻

1. 📍 УГЛУБЛЁННАЯ КЛАССИФИКАЦИЯ СОСТОЯНИЙ (Фрейминг)

Что есть сейчас:
— Эмоции и “важность” сообщения.

Что нужно добавить:
	•	Новые типы внутренних состояний, которые определяют, как человек избегает, контролирует, защищается или застревает:
	•	контроль, избегание, рационализация, стыд, отвержение, сопротивление
	•	Добавить тип фрейма, к которому относится ситуация:
	•	внутренний конфликт, петля, защитная реакция, детская позиция, непроработанная травма и т.п.

📌 Зачем нужно:
Чтобы GPT-SELF понимал не только “что человек чувствует”, но почему он в этом застрял и в каком защитном механизме находится — это делает подсказки точнее, глубже, индивидуальнее.

⸻

2. 🧱 SYSTEM PROMPT BUILDER (Сборка промпта из 5 частей)

Убедиться, что система каждый раз формирует запрос к GPT правильно, в 5 слоях:

№	Слой	Что он делает
1	System.json	Роль GPT: психолог, наблюдатель, ведущий
2	Personalized Prompt	Кто пользователь: его профиль, текущий шаг, ключевые эмоции и смыслы
3	Session Context	В каком режиме человек: шаги / самоанализ / помощь / профиль
4	Helper Prompt	5 важных смыслов (фреймов) из базы, связанных с темой запроса
5	История сообщений	Последние 3–5 сообщений из диалога

📌 Зачем нужно:
Без этой логики GPT не будет помнить важное, давать целостный ответ и сможет “сбиваться”. Система должна точно знать, кто перед ней, что у человека сейчас происходит, и почему он это спрашивает.

⸻

3. 🧠 ENGINE ПАМЯТИ (Volatile / Dynamic / Stable)

Разделить память GPT‑SELF на три уровня:

Уровень памяти	Что хранит	Примеры	Где хранится
Volatile	Текущий диалог	Последние 30–60 минут общения	Dragonfly (TTL)
Dynamic	Повторяющиеся эпизоды, вспышки, эмоции	Например: “Уже 3-й раз он злится на отца”	Dragonfly + БД
Stable	Стабильное ядро личности	Ценности, установки, образы, структура травм	PostgreSQL

📌 Зачем нужно:
Такую модель памяти используют психологи и коучи. Она позволяет видеть как повторы, так и устойчивые убеждения. Это ключ к персонализированному мышлению.

⸻

4. 🧬 ЯДРО ЛИЧНОСТИ (реализация в системе)

Что сейчас:
— Частично формируется из шагов, чувств и истории.

Что нужно:
	•	Возможность вручную “закрепить” Frame как часть ядра личности (например: “всегда обвиняю себя”)
	•	Возможность удалить или “не учитывать” фреймы, которые были временными.
	•	Визуализация прогресса по шагам, чувствам, ключевым изменениям.
	•	Добавить отдельную логику памяти под блок “Рассказать о себе” — он сейчас не учитывается в ядре.

📌 Зачем нужно:
Чтобы в будущем GPT мог ответить:

“Ты часто говоришь о вине в отношениях. Ранее ты уже упоминал это в блоке ‘Работа’ и в шаге 4.”

⸻

5. 🧭 FRAMING ENGINE (движок фрейминга)

Что добавить:
	•	Включить автоматическое определение фрейма для любого сообщения (не только в шагах).
	•	Определять сразу:
	•	Эмоцию
	•	Мотивацию (например, “избежать конфликта”)
	•	Блок (например, “не могу просить помощи”)
	•	Контекст: ситуация или тема
	•	Защитный механизм (например: “обесценивание”)

📌 Зачем нужно:
Чтобы GPT-SELF понимал контекст не хуже, чем психотерапевт.

⸻

6. ⚖️ ДВУХУРОВНЕВЫЙ ОТВЕТ

Формат вывода GPT должен быть в виде JSON:

{
  "emotional": "Мне кажется, ты испытываешь растерянность и напряжение от давления.",
  "structural": "Судя по контексту, ты столкнулся с конфликтом между личной границей и желанием быть полезным."
}

📌 Зачем нужно:
Чтобы пользователь сначала прочитал эмоциональный отклик, а потом структурный анализ. Это помогает и почувствовать, и осознать
⸻




## Этап 3: Админ-панель и роли

**Общая стоимость**: 60,000 - 90,000 руб

### 3.1 Админ-панель (минимальная версия)

**Стоимость**: 30,000 - 60,000 руб

**Цель**: Минимальный веб-интерфейс для управления пользователями и подписками

**Технологии**: FastAPI + простые Jinja2 templates (минимальный дизайн, функциональность важнее)

**Файлы для создания**:

- `twelvesteps/admin/` - новая директория для админки
- `twelvesteps/admin/main.py` - FastAPI приложение для админки
- `twelvesteps/admin/templates/` - простые HTML шаблоны (Bootstrap для базового стиля)
- `twelvesteps/admin/auth.py` - простая аутентификация админов
- `twelvesteps/api/admin.py` - API endpoints для админки

**Минимальный функционал админ-панели**:

1. **Управление пользователями** (базовое):

      - Список пользователей (пагинация, поиск по Telegram ID/username)
      - Просмотр базовой информации пользователя (ID, Telegram ID, дата регистрации, статус подписки)
      - Блокировка/разблокировка пользователя

2. **Управление подписками** (критично):

      - Список всех подписок (активные, истекшие, отмененные)
      - Просмотр деталей подписки (пользователь, план, даты, платеж)
      - Ручная активация/деактивация подписки (для поддержки)
      - История платежей (последние 100 записей)

3. **Базовая статистика**:

      - Общее количество пользователей
      - Количество активных подписок
      - Количество платежей за период

**Аутентификация (минимальная)**:

- Таблица `admins` с полями: `id`, `username`, `password_hash`, `created_at`
- Простая сессия через cookies (без JWT для упрощения)
- Один уровень доступа (все админы имеют одинаковые права)

**Что НЕ включать в минимальную версию**:

- Управление контентом (шаги, вопросы) - через БД напрямую
- Сложная аналитика и графики
- Редактирование данных пользователей (только просмотр)
- Множественные роли админов

**UI в боте (минимальный)**:

- Кнопка "👥 Мои подопечные" в меню спонсора
- Список подопечных с базовой информацией
- Кнопка "Пригласить подопечного" (ввод username)






Следующий этап - Мини ИИ для сжатия промта 






## Этап 4: Безопасность и инфраструктура (MVP Final)

**Общая стоимость**: 120,000 - 180,000 руб

### 1.1 Шифрование данных и соответствие GDPR/152-ФЗ/HIPAA

**Стоимость**: 120,000 - 180,000 руб

**Цель**: Зашифровать все данные, содержащие личную, психологическую или идентифицирующую информацию, с соблюдением требований GDPR, 152-ФЗ и HIPAA

**Чек-лист соответствия GDPR**:

**1. Обработка данных**:

- ✅ Чёткая цель обработки данных (зачем собираем) - документировать в Privacy Policy
- ✅ Пользователь даёт согласие на сбор и обработку - добавить в онбординг
- ✅ Возможность отозвать согласие в любой момент - endpoint для отзыва

**2. Сбор и хранение данных**:

- ✅ Data minimization - собираем только необходимые данные
- ✅ Безопасное хранение - шифрование в БД
- ✅ Data retention policy - логика срока хранения (очистка неактивных аккаунтов)

**3. Безопасность данных**:

- ✅ Шифрование данных в покое (at rest) - шифрование в БД
- ✅ Шифрование данных при передаче (in transit) - HTTPS/TLS
- ✅ Ограничение доступа к данным - только авторизованные пользователи
- ✅ Логирование доступа к данным - таблица AccessLog для аудита

**4. Права пользователя**:

- ✅ Право на доступ к своим данным - endpoint экспорта
- ✅ Право на изменение данных - реализовано через update endpoints
- ✅ Право на удаление данных (Right to be forgotten) - endpoint удаления аккаунта
- ✅ Право на перенос данных (Data portability) - экспорт в JSON
- ✅ Право на ограничение обработки - флаг в UserMeta

**5. Информирование и прозрачность**:

- ✅ Политика конфиденциальности (Privacy Policy) - документ + отображение в боте
- ✅ Ясное объяснение, какие данные собираем и как используем - в онбординге
- ✅ Уведомление о нарушении (data breach) - процедура уведомления в течение 72 часов

**6. Международные передачи данных**:

- ✅ Обеспечение адекватной защиты при передаче за пределы ЕС - хостинг в Европе или специальные контракты

**7. Документация**:

- ✅ Документированная политика безопасности - документы в /docs
- ✅ Учет всех операций с персональными данными - таблица AccessLog
- ⚠️ Назначение DPO (Data Protection Officer) - для больших проектов (опционально)

**Архитектура**:

- Использовать `cryptography` библиотеку (Fernet symmetric encryption с AES-128)
- Мастер-ключ хранить в переменных окружения (`ENCRYPTION_KEY`) или в секретном хранилище (AWS Secrets Manager, HashiCorp Vault)
- Шифровать на уровне сервисов перед сохранением в БД
- Дешифровать при чтении с логированием доступа
- Использовать поле `is_encrypted` для отслеживания статуса шифрования
- Реализовать ротацию ключей шифрования

**Полный список данных для шифрования**:

**Модель User**:

- `telegram_id` - идентификатор Telegram (PII)
- `username` - имя пользователя (PII)
- `first_name` - имя (PII)
- `display_name` - отображаемое имя (PII)
- `personal_prompt` - персонализированный промпт (психологические данные)
- `program_experience` - опыт программы (личная информация)
- `sobriety_date` - дата трезвости (медицинская информация, HIPAA)
- `relapse_dates` - даты срывов (медицинская информация, HIPAA)
- `custom_fields` - кастомные поля (цели, важные люди, форма поддержки - личная информация)

**Модель StepAnswer**:

- `answer_text` - ответы по шагам (психологические данные, HIPAA)

**Модель ProfileAnswer**:

- `answer_text` - ответы в профиле (личная информация)

**Модель ProfileSectionData**:

- `free_text` - свободный текст (личная информация)

**Модель Message**:

- `content` - содержимое сообщений (личная информация, психологические данные)

**Модель Frame**:

- `content` - содержимое frames (память, психологические данные)
- `emotion` - эмоции (психологические данные)
- `thinking_frame` - мыслительный фрейм (психологические данные)
- `strategy_hint` - подсказка стратегии (может содержать личную информацию)

**Модель Gratitude**:

- `text` - текст благодарности (личная информация)

**Модель SessionContext**:

- `context_data` - данные контекста (может содержать личную информацию, если включает HALT, тягу и т.д.)

**Модель TrackerSummary**:

- Поля, содержащие личную информацию о пользователе

**Данные НЕ для шифрования** (метаданные без привязки к пользователю):

- `user_id` (внешний ключ, но не сам идентификатор)
- `created_at`, `updated_at` (метаданные времени)
- `status`, `version` (технические флаги)
- `id` (первичный ключ)
- `user_role` (роль, не идентифицирующая)
- Технические логи без привязки к пользователю

**Файлы для создания/изменения**:

- `twelvesteps/services/encryption_service.py` (новый) - сервис шифрования/дешифрования с ротацией ключей
- `twelvesteps/services/data_protection_service.py` (новый) - сервис для работы с правами пользователей (GDPR)
- `twelvesteps/db/models.py` - добавить поля `is_encrypted` и `encryption_version` в соответствующие таблицы
- `twelvesteps/repositories/*.py` - обновить все репозитории для автоматического шифрования/дешифрования
- `twelvesteps/services/personalization_service.py` - шифровать `personal_prompt`
- `twelvesteps/services/profile.py` - шифровать ответы профиля
- `twelvesteps/api/steps.py` - шифровать ответы по шагам
- `twelvesteps/repositories/GratitudeRepository.py` - шифровать благодарности
- `twelvesteps/repositories/MessageRepository.py` - шифровать сообщения чата
- `twelvesteps/repositories/FrameRepository.py` - шифровать frames
- `twelvesteps/api/data_protection.py` (новый) - endpoints для GDPR прав (экспорт, удаление)

**Реализация EncryptionService**:

```python
# twelvesteps/services/encryption_service.py
from cryptography.fernet import Fernet, MultiFernet
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.kdf.pbkdf2 import PBKDF2HMAC
from cryptography.hazmat.backends import default_backend
import os
import base64
import logging

logger = logging.getLogger(__name__)

class EncryptionService:
    def __init__(self):
        # Поддержка нескольких ключей для ротации
        keys = self._load_encryption_keys()
        if not keys:
            raise ValueError("ENCRYPTION_KEY not set")
        self.cipher = MultiFernet([Fernet(k) for k in keys])
        self.current_key_version = 1
    
    def _load_encryption_keys(self) -> list:
        """Загрузить ключи шифрования (текущий + старые для дешифрования)"""
        current_key = os.getenv("ENCRYPTION_KEY")
        if not current_key:
            return []
        keys = [current_key.encode()]
        # Загрузить старые ключи для ротации (ENCRYPTION_KEY_OLD_1, ENCRYPTION_KEY_OLD_2)
        old_keys = []
        i = 1
        while os.getenv(f"ENCRYPTION_KEY_OLD_{i}"):
            old_keys.append(os.getenv(f"ENCRYPTION_KEY_OLD_{i}").encode())
            i += 1
        return keys + old_keys
    
    def encrypt(self, data: str, key_version: int = None) -> tuple[str, int]:
        """Шифровать данные. Возвращает (encrypted_data, key_version)"""
        if not data:
            return "", 0
        try:
            encrypted = self.cipher.encrypt(data.encode())
            return encrypted.decode(), key_version or self.current_key_version
        except Exception as e:
            logger.error(f"Encryption error: {e}")
            raise
    
    def decrypt(self, encrypted_data: str, key_version: int = None) -> str:
        """Дешифровать данные"""
        if not encrypted_data:
            return ""
        try:
            decrypted = self.cipher.decrypt(encrypted_data.encode())
            return decrypted.decode()
        except Exception as e:
            logger.error(f"Decryption error: {e}")
            # Попытка с другими ключами при ротации
            raise
```

**Миграция БД**:

- Создать Alembic миграцию для добавления полей:
    - `is_encrypted: bool` - флаг шифрования
    - `encryption_version: int` - версия ключа шифрования (для ротации)
- Написать скрипт миграции существующих данных:
    - Шифрование всех существующих записей
    - Обновление флагов `is_encrypted = True`
    - Логирование процесса миграции

**Переменные окружения**:

- `ENCRYPTION_KEY` - текущий ключ (сгенерировать через `Fernet.generate_key()`)
- `ENCRYPTION_KEY_OLD_1`, `ENCRYPTION_KEY_OLD_2` - старые ключи для ротации (опционально)

**Соответствие законодательству**:

**GDPR (General Data Protection Regulation)**:

- Право на доступ (статья 15): Endpoint `GET /data-protection/export` - экспорт всех данных пользователя
- Право на удаление (статья 17): Endpoint `DELETE /data-protection/delete-account` - полное удаление данных
- Право на исправление (статья 16): Реализовано через обычные update endpoints
- Право на ограничение обработки (статья 18): Флаг `processing_restricted` в `UserMeta`
- Право на переносимость данных (статья 20): Экспорт в структурированном формате (JSON)
- Согласие на обработку (статья 7): Таблица `consents` для хранения согласий пользователя
- Уведомление о нарушениях (статья 33): Логирование и уведомление при утечках данных

**152-ФЗ (Россия)**:

- Согласие субъекта персональных данных (статья 9): Таблица `consents` с типом согласия
- Право на получение информации (статья 14): Endpoint для получения информации об обработке
- Право на отзыв согласия (статья 9): Endpoint `POST /data-protection/revoke-consent`
- Уведомление Роскомнадзора (статья 22): Документация процесса уведомления
- Локализация данных (статья 18): Хранение данных российских пользователей в РФ

**HIPAA (Health Insurance Portability and Accountability Act)**:

- Шифрование данных в покое (45 CFR § 164.312): Реализовано через шифрование в БД
- Шифрование данных при передаче (45 CFR § 164.312): HTTPS/TLS для всех API запросов
- Контроль доступа (45 CFR § 164.312): Аудит доступа к данным, логирование
- Аудит (45 CFR § 164.312): Таблица `access_logs` для логирования доступа к медицинским данным
- Минимально необходимое использование (45 CFR § 164.502): Принцип минимального доступа к данным

**Файлы для соответствия законодательству**:

- `twelvesteps/db/models.py` - добавить модель `Consent` для согласий
- `twelvesteps/db/models.py` - добавить модель `AccessLog` для аудита доступа
- `twelvesteps/services/data_protection_service.py` - сервис для работы с правами пользователей
- `twelvesteps/api/data_protection.py` - endpoints для GDPR/152-ФЗ прав
- `twelvesteps/services/audit_service.py` (новый) - сервис для логирования доступа к данным
- `docs/PRIVACY_POLICY.md` - политика конфиденциальности
- `docs/DATA_PROCESSING_AGREEMENT.md` - соглашение об обработке данных
- `docs/GDPR_COMPLIANCE.md` - документация по соответствию GDPR
- `docs/HIPAA_COMPLIANCE.md` - документация по соответствию HIPAA

**Модель Consent**:

```python
class Consent(Base):
    __tablename__ = "consents"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    consent_type: Mapped[str]  # data_processing, marketing, third_party
    granted: Mapped[bool]
    granted_at: Mapped[Optional[datetime]]
    revoked_at: Mapped[Optional[datetime]]
    ip_address: Mapped[Optional[str]]
    user_agent: Mapped[Optional[str]]
    created_at: Mapped[datetime]
```

**Модель AccessLog**:

```python
class AccessLog(Base):
    __tablename__ = "access_logs"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    accessed_by: Mapped[Optional[int]]  # user_id или admin_id
    access_type: Mapped[str]  # read, update, delete, export
    resource_type: Mapped[str]  # profile, step_answer, message, etc.
    resource_id: Mapped[Optional[int]]
    ip_address: Mapped[Optional[str]]
    user_agent: Mapped[Optional[str]]
    created_at: Mapped[datetime]
```

**Endpoints для прав пользователей**:

- `GET /data-protection/export` - экспорт всех данных пользователя (GDPR Article 15, 152-ФЗ Article 14)
- `DELETE /data-protection/delete-account` - полное удаление аккаунта и данных (GDPR Article 17)
- `POST /data-protection/consent` - дать согласие на обработку данных
- `POST /data-protection/revoke-consent` - отозвать согласие (GDPR Article 7, 152-ФЗ Article 9)
- `GET /data-protection/consents` - получить список согласий
- `POST /data-protection/restrict-processing` - ограничить обработку данных (GDPR Article 18)
- `GET /data-protection/access-logs` - получить логи доступа к данным (HIPAA audit)

**Технические меры безопасности**:

- HTTPS/TLS для всех соединений (обязательно для HIPAA)
- Rate limiting для API endpoints
- Валидация всех входных данных
- Регулярное обновление зависимостей
- Резервное копирование зашифрованных данных
- Мониторинг подозрительной активности
- Двухфакторная аутентификация для админов (опционально)

---


### 2.3 Интеграция Cryptomus (Международная + крипто)

**Стоимость**: 6,000 - 12,000 руб - опционально

**Цель**: Прием платежей через Cryptomus для зарубежных пользователей и криптовалют

**Документация**: https://doc.cryptomus.com/

**Файлы для создания**:

- `twelvesteps/services/payment/cryptomus_service.py` - сервис работы с Cryptomus API
- Обновить `twelvesteps/api/payment.py` - добавить endpoints для Cryptomus

**Flow платежа** (аналогично ЮKassa):

1. Создание платежа через Cryptomus API
2. Получение ссылки на оплату
3. Обработка webhook после оплаты
4. Активация подписки

**Endpoints**:

- `POST /payment/cryptomus/create` - создать платеж
- `POST /payment/cryptomus/webhook` - обработка webhook
- `GET /payment/cryptomus/status/{payment_id}` - проверка статуса

**Переменные окружения**:

- `CRYPTOMUS_API_KEY` - API ключ
- `CRYPTOMUS_MERCHANT_ID` - ID мерчанта

---



### 3.2 Роль "Спонсор" (минимальная реализация)

**Стоимость**: 30,000 руб

**Цель**: Минимальный функционал для пользователей с ролью `sponsor`

**Текущее состояние**: В `UserRole` enum уже есть `sponsor`, но функционал не реализован

**Файлы для изменения**:

- `twelvesteps/db/models.py` - добавить модель `SponsorRelationship`
- `twelvesteps/services/sponsor_service.py` - минимальный сервис для работы со спонсорами
- `twelvesteps/api/sponsor.py` - базовые endpoints для спонсоров
- `twelvesteps_tgbot/bot/handlers.py` - handlers для спонсорского функционала

**Минимальный функционал спонсора**:

1. **Связь с подопечными** (базовое):

      - Приглашение подопечного (по Telegram username)
      - Подтверждение связи от подопечного
      - Список активных подопечных

2. **Просмотр базовой информации** (только с согласия подопечного):

      - Статус прогресса по шагам (сколько шагов пройдено, без деталей ответов)
      - Общая статистика активности (дата последнего входа, количество дней активности)

**Что НЕ включать в минимальную версию**:

- Просмотр детальных ответов подопечного (приватность)
- Внутренний чат через бота (можно общаться в Telegram напрямую)
- Уведомления о событиях (сложная логика)
- Анонимные инсайты (требует дополнительной обработки данных)

**Модель связи**:

```python
class SponsorRelationship(Base):
    __tablename__ = "sponsor_relationships"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    sponsor_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    dependent_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    status: Mapped[str] = mapped_column(String(20))  # pending, active, ended
    dependent_consent: Mapped[bool] = mapped_column(default=False)  # согласие на просмотр данных
    created_at: Mapped[datetime] = mapped_column(server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(server_default=func.now(), onupdate=func.now())
    
    # Relationships
    sponsor: Mapped["User"] = relationship(foreign_keys=[sponsor_id])
    dependent: Mapped["User"] = relationship(foreign_keys=[dependent_id])
```







## Приоритеты и последовательность.   Этот раздел не знаю нужен ли и если нужен в каком формате.

### Фаза 1 (Критично для MVP):

1. **Шифрование данных** - все личные, психологические и идентифицирующие данные
2. **GitHub структура** - организация репозитория, документация
3. **Автодеплой на Render** - настройка CI/CD
4. **Модель подписки в БД** - таблица subscriptions, связь с User
5. **Интеграция оплаты (ЮKassa)** - минимальная реализация для приема платежей

### Фаза 2 (Монетизация):

1. **UI подписки в боте** - меню подписки, статус, оформление
2. **Платные функции** - минимальная реализация проверки подписки и ограничений

### Фаза 3 (Расширение - минимально):

1. **Админ-панель (базовая версия)** - минимальный функционал (управление пользователями и подписками)
2. **Роль Спонсор** - минимальная реализация (связь спонсор-подопечный, базовый просмотр прогресса)

---

## Обратная связь и рекомендации

### Критические моменты:

1. **Шифрование**: 

      - Шифровать ВСЕ данные, содержащие личную, психологическую или идентифицирующую информацию
      - Обязательно протестировать производительность после внедрения
      - Использовать асинхронное шифрование/дешифрование где возможно

2. **Оплата**:

      - Начать с ЮKassa (российский рынок приоритетен)
      - Cryptomus добавить позже, когда будет международная аудитория
      - Обязательно протестировать webhook обработку в тестовом режиме
      - Цена подписки: **390₽/мес** (или эквивалент)

3. **Подписки**:

      - Пробный период опционален (можно добавить позже)
      - Автоматические напоминания о продлении за 3 дня до истечения
      - Автоматическое отключение платных функций при истечении подписки

4. **Админ-панель**:

      - Минимальная версия: только управление пользователями и подписками
      - Простой дизайн (Bootstrap), функциональность важнее красоты
      - Расширять функционал по мере необходимости

5. **Спонсор**:

      - Минимальная версия: только связь спонсор-подопечный и базовый просмотр прогресса
      - Обязательно получить согласие подопечного на просмотр данных
      - Не показывать детальные ответы (приватность)

### Технические рекомендации:

1. **Производительность**:

      - Оптимизировать запросы к БД (индексы, eager loading)
      - Кэширование для проверки подписки (чтобы не делать запрос к БД при каждом обращении)
      - Асинхронное шифрование/дешифрование

2. **Мониторинг**:

      - Настроить логирование ошибок (можно использовать Render Logs или Sentry)
      - Базовые метрики: количество пользователей, активных подписок, платежей
      - Мониторинг критичных операций (платежи, активация подписок)

3. **Безопасность**:

      - Rate limiting для API endpoints (особенно для payment webhooks)
      - Валидация всех входных данных
      - Регулярное обновление зависимостей
      - HTTPS/TLS для всех соединений (обязательно)

### Временные и стоимостные оценки:

**Фаза 1** (Шифрование + GitHub + Деплой + Подписки + ЮKassa): 
- Время: 2-3 недели
- Стоимость: 150,000 - 228,000 руб

**Фаза 2** (UI подписки + Платные функции): 
- Время: 1-2 недели
- Стоимость: 6,000 - 12,000 руб

**Фаза 3** (Админка + Спонсор): 
- Время: 1-2 недели
- Стоимость: 60,000 - 90,000 руб

**Этап 5** (Отладка и логирование):
- Время: 0.5-1 день
- Стоимость: 3,000 - 6,000 руб

**Итого**: 
- Время: 4-7 недель до готовности MVP с монетизацией
- Стоимость: 219,000 - 336,000 руб (без учета семантического ядра и Cryptomus)

**С учетом всех опциональных этапов**:
- Стоимость: 243,000 - 378,000 руб (включая семантическое ядро и Cryptomus)

---

## Этап 5: Отладка и контроль сохранений (Debugging & Logging)

**Стоимость**: 3,000 - 6,000 руб

### Цель
Сделать поведение системы объяснимым: видеть, что сохраняется, какие фреймы/эмоции определяются, как формируется промпт, и какие действия GPT предпринимает.

### 1.1 Создание таблицы логов в PostgreSQL

**Цель**: Хранить логи всех обращений к GPT для отладки и анализа

**Файлы для создания/изменения**:
- `twelvesteps/db/models.py` - добавить модель `UserLog`
- `twelvesteps/alembic/versions/XXX_add_user_logs.py` - миграция для создания таблицы

**Модель UserLog**:
```python
class UserLog(Base):
    __tablename__ = "user_logs"
    
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), index=True)
    timestamp: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    message: Mapped[Optional[str]] = mapped_column(Text, nullable=True)  # Входное сообщение пользователя
    step: Mapped[Optional[str]] = mapped_column(String(50), nullable=True)  # Текущий шаг/контекст
    emotion: Mapped[Optional[List[str]]] = mapped_column(JSON, nullable=True)  # Определенные эмоции
    frame: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)  # Определенный фрейм
    tags: Mapped[Optional[List[str]]] = mapped_column(JSON, nullable=True)  # Семантические теги
    prompt_excerpt: Mapped[Optional[str]] = mapped_column(Text, nullable=True)  # Первые 500 символов промпта
    model_used: Mapped[Optional[str]] = mapped_column(String(50), nullable=True)  # Использованная модель GPT
    tokens_used: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)  # Количество токенов
    
    # Relationships
    user: Mapped["User"] = relationship()
```

**Индексы**:
- `user_id` + `timestamp` для быстрого поиска логов пользователя
- `emotion` (GIN индекс для массива) для поиска по эмоциям
- `tags` (GIN индекс для массива) для поиска по тегам

### 1.2 Сохранение лога при каждом обращении к GPT

**Цель**: Автоматически логировать все обращения к GPT

**Файлы для изменения**:
- `twelvesteps/core/chat_service.py` - добавить вызов сохранения лога после каждого запроса к GPT
- `twelvesteps/services/personalization_service.py` - логировать формирование промпта
- `twelvesteps/repositories/UserLogRepository.py` (новый) - CRUD операции для логов

**Логика сохранения**:
- Сохранять лог после каждого успешного запроса к GPT
- Включать: входное сообщение, контекст (шаг/SOS/профиль), определенные эмоции/фреймы/теги, фрагмент промпта, модель, токены
- Логировать только успешные запросы (не логировать ошибки API, чтобы не засорять БД)

**Пример вызова**:
```python
await user_log_repository.save_log(
    user_id=user.id,
    message=user_input,
    step=current_step_context,
    emotion=detected_emotions,
    frame=detected_frame,
    tags=detected_tags,
    prompt_excerpt=full_prompt[:500],
    model_used="gpt-4o-mini",
    tokens_used=usage.total_tokens
)
```

### 1.3 API endpoints для просмотра логов

**Цель**: Предоставить доступ к логам через API

**Файлы для создания/изменения**:
- `twelvesteps/api/debug.py` (новый) - endpoints для отладки
- `twelvesteps/api/schemas.py` - добавить схемы для логов

**Endpoints**:
- `GET /debug/logs/{user_id}` - получить последние N логов пользователя (по умолчанию 20)
- `GET /debug/logs/{user_id}?limit=50&offset=0` - пагинация
- `GET /debug/logs/{user_id}?emotion=вина` - фильтрация по эмоциям
- `GET /debug/logs/{user_id}?tag=работа` - фильтрация по тегам
- `GET /debug/stats/{user_id}` - агрегированная статистика (частота эмоций, тегов, использование токенов)

**Безопасность**:
- Доступ только для самого пользователя или администратора
- Использовать существующую систему аутентификации

### 1.4 Команда в Telegram для администратора

**Цель**: Быстрый доступ к логам через бота

**Файлы для изменения**:
- `twelvesteps_tgbot/bot/handlers.py` - добавить handler для команды `/debug_last`
- `twelvesteps_tgbot/bot/backend.py` - добавить метод для получения логов

**Команда**:
- `/debug_last` - показать последние 10 логов текущего пользователя
- `/debug_last {user_id}` - показать последние 10 логов указанного пользователя (только для админов)

**Формат вывода**:
```
Последние логи:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2025-12-25 12:00:00]
Сообщение: "Я чувствую вину..."
Шаг: STEP_1
Эмоции: вина, тревога
Фрейм: избегание
Теги: работа, границы
Модель: gpt-4o-mini
Токены: 150
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 1.5 Оптимизация и ограничения

**Лимиты**:
- Хранить логи не более 90 дней (автоматическая очистка старых логов)
- Ограничить размер `prompt_excerpt` до 500 символов
- Индексировать только часто используемые поля

**Производительность**:
- Использовать асинхронное сохранение логов (не блокировать основной поток)
- Batch insert для массовых операций
- Партиционирование таблицы по `timestamp` (опционально, для больших объемов)

**На будущее** (опционально):
- Логировать выборочно (только STEP, SOS, PROFILE) для экономии места
- Поиск по эмоциям/тегам через PostgreSQL массивы
- Агрегация статистики через GROUP BY
- Лимиты по запросам (подсчет за сутки и сравнение с лимитом подписки)

**Файлы для создания/изменения**:
- `twelvesteps/services/log_cleanup_service.py` (новый) - сервис для очистки старых логов
- `twelvesteps/api/scheduled_tasks.py` (новый) - scheduled task для периодической очистки

**Временная оценка**: 0.5-1 день

